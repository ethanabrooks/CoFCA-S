#!/usr/bin/env zsh

config=$(${0:a:h}/search $2 config.yaml)
load=$(${0:a:h}/search $2 checkpoint.pt)
#failure_buffer_load_path=$(${0:a:h}/search $2 failure_buffer.pkl)
cmd="python $1 --config $config --load $load --no_wandb ${@:3}"
echo "$cmd\n"
eval "$cmd"
