# An unique identifier for the head node and workers of this cluster.
cluster_name: local-default

## NOTE: Typically for local clusters, min_workers == initial_workers == max_workers.

# The minimum number of workers nodes to launch in addition to the head
# node. This number should be >= 0.
# Typically, min_workers == initial_workers == max_workers.
min_workers: 0
# The initial number of worker nodes to launch in addition to the head node.
# Typically, min_workers == initial_workers == max_workers.
initial_workers: 56

# The maximum number of workers nodes to launch in addition to the head node.
# This takes precedence over min_workers.
# Typically, min_workers == initial_workers == max_workers.
max_workers: 70

# Autoscaling parameters.
# Ignore this if min_workers == initial_workers == max_workers.
autoscaling_mode: default
target_utilization_fraction: 0.8
idle_timeout_minutes: 5

# This executes all commands on all nodes in the docker container,
# and opens all the necessary ports to support the Ray cluster.
# Empty string means disabled. Assumes Docker is installed.
docker:
    image: ethanabrooks/ppo-repo:latest
    container_name: latest
    run_options: []  # Extra options to pass into "docker run"

# Local specific configuration.
provider:
    type: local
    head_ip: 141.212.113.98
    worker_ips: 
    # 141.212.113.88 # rldl1 down
    - 141.213.3.138 # rldl2 
    # 141.213.3.134 # rldl3 risto
    #- 141.213.3.21 # rldl4 
    # 141.213.3.28 # rldl5 no access
    - 141.213.3.27 # rldl6 
    - 141.213.3.130 # rldl7 
    - 141.213.3.4 # rldl8 
    - 141.212.113.254 # rldl9 
    # 141.212.113.23 # rldl10 Max
    - 141.212.113.249 # rldl11 
    #- 141.212.113.98 # rldl12  HEAD
    - 141.213.3.71 # rldl13 
    #- 141.212.113.59 # rldl14 
    #- 141.212.113.250 # rldl15 
    #- 141.212.113.173 # rldl16 
    # 141.212.113.180 # rldl17



# How Ray will authenticate with newly launched nodes.
auth:
    ssh_user: ethanbro
    ssh_private_key: ~/.ssh/id_rsa

# Leave this empty.
head_node: {}

# Leave this empty.
worker_nodes: {}

# Files or directories to copy to the head and worker nodes. The format is a
# dictionary from REMOTE_PATH: LOCAL_PATH, e.g.
file_mounts: {}
#    "/path1/on/remote/machine": "/path1/on/local/machine",
#    "/path2/on/remote/machine": "/path2/on/local/machine",

# List of commands that will be run before `setup_commands`. If docker is
# enabled, these commands will run outside the container and before docker
# is setup.
initialization_commands: 
  - docker pull ethanabrooks/ppo-repo:latest

# List of shell commands to run to set up each nodes.
# Runs first inside docker
setup_commands: []

# Custom commands that will be run on the head node after common setup.
# Runs second inside docker for head
head_setup_commands: []

# Custom commands that will be run on worker nodes after common setup.
# Runs second inside docker for worker
worker_setup_commands: []

# Command to start ray on the head node. You don't need to change this.
# Runs last inside docker for head
head_start_ray_commands: 
    - ray stop
    - ulimit -c unlimited && ray start --head --redis-port=6379 --autoscaling-config=~/ray_bootstrap_config.yaml
    - bash tune.sh

# Command to start ray on worker nodes. You don't need to change this.
# Runs last inside docker for worker
worker_start_ray_commands:
    - ray stop
    - ray start --redis-address=$RAY_HEAD_IP:6379
    - bash tune.sh
